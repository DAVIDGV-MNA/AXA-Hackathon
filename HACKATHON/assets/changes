def chunk_list(seq, n):
    """Yield successive n-sized chunks from seq. n must be > 0."""
    if n <= 0:
        raise ValueError("chunk size n must be > 0")
    for i in range(0, len(seq), n):
        yield seq[i:i + n]