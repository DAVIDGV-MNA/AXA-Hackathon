def fuzzy_title_hits(proposed_title: str, cutoff: float = 0.84) -> List[Dict[str, Any]]:
+    """Treat title-search via RAG as the fuzzy pass and keep results ≥ cutoff."""
+    if not (proposed_title or "").strip():
+        return []
+    hits = cosine_search(proposed_title, top_k=8)
+    return [h for h in hits if h["score"] >= cutoff]