const { api_version, url_version, embeddings_url, model_url, model, input } = req.body;
const embUrl = buildUrl(embeddings_url || model_url || SECUREGPT_URL_EMBEDDINGS, {
  apiVersion: api_version || SECUREGPT_API_VERSION,
  urlVersion: url_version || SECUREGPT_URL_VERSION
});
